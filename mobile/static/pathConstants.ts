import { join } from "path";

export const ANDROID_APP_PATH = join(
  process.cwd(),
  "app",
  "android",
  "Final_Exam_MacroDroid.apk"
);
export const JSON_OUTPUT_DIR = join(process.cwd(), "reports");
